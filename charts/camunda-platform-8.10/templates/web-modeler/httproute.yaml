{{- if and .Values.global.gateway.enabled (not .Values.global.gateway.external) -}}
{{- if eq (include "camundaHub.webModelerEnabled" .) "true" }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "camundaPlatform.fullname" . }}-modeler
  annotations:
    {{- with .Values.global.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.global.gateway.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  labels:
    {{- include "common.tplvalues.merge-overwrite" (dict
      "values" (list
        (include "camundaPlatform.labels" .)
        .Values.global.gateway.labels
      )
      "context" .
    ) | nindent 4 }}
spec:
  parentRefs:
  - name: {{ include "camundaPlatform.fullname" . }}
    sectionName: {{ if .Values.global.gateway.tls.enabled }}https{{ else }}http{{ end }}
  hostnames:
  - {{ tpl .Values.global.host $ | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: {{ (or .Values.camundaHub.webModeler.contextPath .Values.webModeler.contextPath) }}
    backendRefs:
    - name: {{ template "webModeler.restapi.fullname" . }}
      namespace: {{ .Release.Namespace }}
      port: {{ (dig "webModeler" "restapi" "service" "port" .Values.webModeler.restapi.service.port .Values.camundaHub) }}
  - matches:
    - path:
        type: PathPrefix
        value: {{ template "webModeler.websocketContextPath" . }}
    backendRefs:
    - name: {{ template "webModeler.websockets.fullname" . }}
      namespace: {{ .Release.Namespace }}
      port: {{ (dig "webModeler" "websockets" "service" "port" .Values.webModeler.websockets.service.port .Values.camundaHub) }}
{{- end }}
{{- end }}
