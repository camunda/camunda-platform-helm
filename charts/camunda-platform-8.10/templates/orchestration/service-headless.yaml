{{/*
The headless service is used for Zeebe brokers Pod-to-Pod communication.
*/}}
{{- if eq (include "camundaPlatform.orchestrationEnabled" .) "true" -}}
{{- include "orchestration.renderHeadlessServices" (dict "context" .) -}}
{{- end -}}
