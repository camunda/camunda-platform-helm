{{- if eq (include "camundaPlatform.orchestrationEnabled" .) "true" -}}
{{- include "orchestration.renderBrokerGenerations" (dict "manifest" "orchestration.configmapManifest" "context" .) -}}
{{- end -}}
