# File secret store configuration for centralized secret resolution.

orchestration:
  secretStore:
    file:
      default:
        path: /etc/camunda/secrets
        existingSecret: camunda-secret-store-test
