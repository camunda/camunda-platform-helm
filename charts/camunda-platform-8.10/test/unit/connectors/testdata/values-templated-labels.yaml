global:
  elasticsearch:
    enabled: true


orchestration:
  data:
    secondaryStorage:
      type: elasticsearch

connectors:
  enabled: true
  podLabels:
    release: "{{ .Release.Name }}"
  podAnnotations:
    release: "{{ .Release.Name }}"
