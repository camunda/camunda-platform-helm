# Test values for global.host templating in Console ConfigMap (releaseInfo)
global:
  host: "{{ .Release.Name }}.example.com"
  ingress:
    enabled: true
    tls:
      enabled: true

console:
  enabled: true

identity:
  enabled: true

orchestration:
  enabled: true
