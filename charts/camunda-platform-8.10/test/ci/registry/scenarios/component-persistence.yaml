name: component-persistence
auth: keycloak
skip-e2e: true
flows:
  - install
identity: keycloak
persistence: elasticsearch
features:
  - persistence
platforms:
  - gke
infra-type:
  gke: distroci
dependencies:
  - keycloak
  - elasticsearch
  - postgresql
