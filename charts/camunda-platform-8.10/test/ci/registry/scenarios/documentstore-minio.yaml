name: documentstore-minio
auth: basic
flows:
  - install
identity: basic
persistence: elasticsearch
skip-e2e: true
features:
  - documentstore-minio
exclude:
  - orchestration-grpc
platforms:
  - gke
infra-type:
  gke: distroci
pre-install: documentstore-minio
post-deploy: documentstore-minio-verify
dependencies:
  - elasticsearch
