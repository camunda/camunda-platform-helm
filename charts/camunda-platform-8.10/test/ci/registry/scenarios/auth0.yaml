name: auth0
auth: oidc
flows:
  - install
identity: auth0
persistence: elasticsearch
features:
  - hub-ping
  - hub-ping-token-params-test-image
platforms:
  - gke
exclude:
  - identity
  - console
  - orchestration-grpc
infra-type:
  eks: preemptible
  gke: distroci
post-deploy: hub-ping
dependencies:
  - elasticsearch
  - postgresql
