name: auth0
auth: oidc
flows:
  - install
identity: auth0
persistence: elasticsearch
platforms:
  - gke
exclude:
  - identity
  - console
  - orchestration-grpc
infra-type:
  eks: preemptible
  gke: distroci
pre-install: cnpg
dependencies:
  - elasticsearch
