name: auth0
auth: oidc
flows:
  - install
identity: auth0
persistence: elasticsearch
features:
  - hub-ping
extra-values:
  - values/auth0-hub-ping-test-image.yaml
platforms:
  - gke
exclude:
  - identity
  - console
  - orchestration-grpc
infra-type:
  eks: preemptible
  gke: distroci
post-deploy: hub-ping
dependencies:
  - elasticsearch
  - postgresql
