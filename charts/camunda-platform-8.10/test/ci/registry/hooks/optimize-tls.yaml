script: pre-install-optimize-tls.sh
description: |
  Generates a self-signed certificate/key pair for the Optimize HTTP
  endpoint and packages it as a PKCS12 keystore in an `optimize-tls-keystore`
  Secret consumed by `global.tls.optimize.cert.secret.existingSecret`.
