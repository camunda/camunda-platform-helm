apiVersion: v2
name: camunda-platform
version: 13.0.0-alpha8
appVersion: 8.8.x
description: |
  Camunda 8 Self-Managed Helm charts.
  Camunda's process automation platform allows developers to design, automate and improve processes.
type: application
icon: https://helm.camunda.io/imgs/camunda.svg
home: https://docs.camunda.io/docs/self-managed/overview/
sources:
  - https://github.com/camunda/camunda-platform-helm
keywords:
  - automation
  - business-rules
  - bpm
  - bpmn
  - camunda-platform
  - decision-engine
  - dmn
  - modeling
  - orchestration
  - process-automation
  - process-engine
  - process-orchestration
  - workflow
  - workflow-engine
dependencies:
  # Identity Dependencies.
  - name: keycloak
    alias: identityKeycloak
    repository: oci://registry-1.docker.io/bitnamicharts
    version: 24.9.0
    condition: "identityKeycloak.enabled"
  - name: postgresql
    alias: identityPostgresql
    repository: oci://registry-1.docker.io/bitnamicharts
    version: 15.x.x
    condition: "identityPostgresql.enabled"
  # WebModeler Dependencies.
  - name: web-modeler-postgresql
    alias: webModelerPostgresql
    repository: "file://../web-modeler-postgresql-15"
    version: 15.x.x
    condition: "webModelerPostgresql.enabled"
  # Shared Dependencies.
  - name: elasticsearch
    repository: oci://registry-1.docker.io/bitnamicharts
    version: 21.6.3
    condition: "elasticsearch.enabled"
  # Helpers.
  - name: common
    repository: oci://registry-1.docker.io/bitnamicharts
    version: 2.x.x
maintainers:
  - name: aabouzaid
    email: ahmed.abouzaid@camunda.com
  - name: drodriguez-305
    email: daniel.rodriguez@camunda.com
  - name: hamza-m-masood
    email: hamza.masood@camunda.com
  - name: jessesimpson36
    email: jesse.simpson@camunda.com
annotations:
  artifacthub.io/prerelease: "true"
  camunda.io/helmCLIVersion: "3.18.6"
  artifacthub.io/links: |
    - name: Camunda 8 docs
      url: https://docs.camunda.io/
    - name: Camunda 8 Self-Managed docs
      url: https://docs.camunda.io/docs/self-managed/about-self-managed/
    - name: Camunda 8 Self-Managed Helm charts
      url: https://github.com/camunda/camunda-platform-helm
  artifacthub.io/containsSecurityUpdates: "false"
  artifacthub.io/changes: |
    - kind: added
      description: "Adding extra configuration options to the process migration"
    - kind: added
      description: "Backport - implement joinPath helper for constructing paths"
    - kind: added
      description: "Add usage metrics retention configuration"
    - kind: added
      description: "Create extra values file for legacy bitnami repo name"
    - kind: added
      description: "1898 enhancement inconsistent inputs for existingsecret"
    - kind: added
      description: "Add schema validation for env keys"
    - kind: changed
      description: "**⚠ BREAKING CHANGE** Rename core to orchestration"
    - kind: changed
      description: "Rename templates dir and add release highlights"
    - kind: changed
      description: "Set orchestration retention and history retention"
    - kind: changed
      description: "Move global.security.* to orchestration.security.*"
    - kind: changed
      description: "Rename connectors config file from yml to yaml"
    - kind: changed
      description: "Revert to zeebe as name for orchestration cluster"
    - kind: fixed
      description: "Align ports and minor localhost-port-forward fixes"
    - kind: fixed
      description: "Malformed issuer-uri"
    - kind: fixed
      description: "TODO comment to add issuer backend URL to orchestration configmap"
    - kind: fixed
      description: "The ttl was blank causing the namespaces to never be cleaned up.…"
    - kind: fixed
      description: "Fixup missing references to global.multitenancy.enabled"
    - kind: fixed
      description: "Missing else in identity multitenancy configuration"
    - kind: fixed
      description: "Add checksum annotations to migration pods"
    - kind: fixed
      description: "The region is mapped to the document store env"
    - kind: fixed
      description: "Use orchestration instead of core for configmap annotation"
    - kind: fixed
      description: "Disable persistent sessions in noSecondaryStorage mode"
    - kind: fixed
      description: "Add optimize and web modeler constraint requiring management identity"
    - kind: fixed
      description: "Refactor restapi pusher secret to new secret convention"
    - kind: fixed
      description: "Moved global.secutity to orchestration.security"
    - kind: fixed
      description: "Define one security section in orchestration configmap"
    - kind: fixed
      description: "Set orchestration cluster sts matchLabels to zeebe-broker"
    - kind: fixed
      description: "Set the groups claim empty so groups can be tested on the orchId…"
    - kind: fixed
      description: "Update the role name to match the snapshot updates"
    - kind: fixed
      description: "Added missing config to orchestration cluster"
    - kind: fixed
      description: "Refactor autogenerated secrets"
    - kind: fixed
      description: "Revert webmodeler externaldb change"
