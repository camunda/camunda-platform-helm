{{- if .Values.execution-identity.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "execution-identity.fullname" . }}
  labels:
    {{- include "execution-identity.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.global.annotations}}
      {{- toYaml .Values.global.annotations | nindent 4 }}
    {{- end }}
    {{- if .Values.execution-identity.service.annotations}}
      {{- toYaml .Values.execution-identity.service.annotations | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.execution-identity.service.type }}
  ports:
    - port: {{ .Values.execution-identity.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.execution-identity.service.managementPort }}
      name: management
      targetPort: management
      protocol: TCP
  selector:
    {{- include "execution-identity.matchLabels" . | nindent 4 }}
{{- end }}
