prometheus:
  ingress:
    enabled: true
    hosts:
      - prometheus-{{ .Values.host }}
    tls:
      - secretName: integration-test
        hosts:
          - prometheus-{{ .Values.host }}
prometheusOperator:
  admissionsWebhooks:
    enabled: false
