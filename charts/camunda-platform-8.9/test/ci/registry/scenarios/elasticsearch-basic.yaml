name: elasticsearch-basic
auth: basic
flows:
  - install
identity: basic
persistence: elasticsearch
exclude:
  - identity
  - console
  - orchestration-grpc
infra-type:
  eks: preemptible
  gke: distroci
