global:
  elasticsearch:
    enabled: true

elasticsearch:
  enabled: true

orchestration:
  data:
    secondaryStorage:
      type: elasticsearch

identity:
  enabled: true

console:
  enabled: true
  podLabels:
    release: "{{ .Release.Name }}"
  podAnnotations:
    release: "{{ .Release.Name }}"
