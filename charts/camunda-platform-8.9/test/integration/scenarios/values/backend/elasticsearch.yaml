# Elasticsearch backend
# Layer 3: Data backend
# Used when TEST_BACKEND=elasticsearch

global:
  elasticsearch:
    enabled: true

elasticsearch:
  enabled: true
  maxUnavailable: 0

orchestration:
  data:
    secondaryStorage:
      type: elasticsearch
      elasticsearch:
        url: "http://integration-elasticsearch:9200"
