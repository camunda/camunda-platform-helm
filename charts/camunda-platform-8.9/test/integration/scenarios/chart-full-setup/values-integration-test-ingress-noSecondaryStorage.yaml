global:
  elasticsearch:
    enabled: false

  noSecondaryStorage: true

elasticsearch:
  enabled: false

orchestration:
  data:
    secondaryStorage:
      elasticsearch:
        auth:
          username: elastic
          secret:
            existingSecret: infra-credentials
            existingSecretKey: elasticsearch-password
