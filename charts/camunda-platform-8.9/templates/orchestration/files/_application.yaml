{{/*
This file name should start with an underscore to avoid rendering it as a template.
It is included (imported) in the ConfigMap.
*/}}
spring:
  profiles:
    active: {{ include "orchestration.enabledProfilesWithIdentity" . | quote }}
  servlet:
    multipart:
      max-file-size: {{ .Values.global.config.requestBodySize | quote }}
      max-request-size: {{ .Values.global.config.requestBodySize | quote }}

server:
  address: 0.0.0.0
  port: {{ .Values.orchestration.service.httpPort }}
  {{- if .Values.orchestration.contextPath }}
  servlet:
    context-path: {{ include "camundaPlatform.joinpath" (list .Values.orchestration.contextPath) | quote }}
  {{- end }}

management:
  server:
    address: 0.0.0.0
    port: {{ .Values.orchestration.service.managementPort }}
    base-path: {{ include "camundaPlatform.joinpath" (list .Values.orchestration.contextPath) | quote }}

camunda:
  {{- if .Values.orchestration.exporters.rdbms.enabled }}
  backup:
    webapps:
      enabled: false
  {{- end }}
  license:
    key: "${VALUES_ORCHESTRATION_LICENSE_KEY}"

  system:
    cpu-thread-count: {{ .Values.orchestration.cpuThreadCount }}
    io-thread-count: {{ .Values.orchestration.ioThreadCount }}
    clock-controlled: false
    validate-restore-config: true
    upgrade:
    {{- if .Values.orchestration.upgrade.allowPreReleaseImages }}
      enable-version-check: false
    {{- else }}
      enable-version-check: true
    {{- end }}

  cluster:
    # The Node ID depends on the StatefulSet Pod's name so it cannot be templated in the StatefulSet level.
    # The value of "node-id" is calculated in the "startup.sh" file and exported as "VALUES_ORCHESTRATION_NODE_ID" env var.
    node-id: "${VALUES_ORCHESTRATION_NODE_ID:}"
    size: {{ .Values.orchestration.clusterSize | quote }}
    replication-factor: {{ .Values.orchestration.replicationFactor | quote }}
    partition-count: {{ .Values.orchestration.partitionCount | quote }}
    # zeebe.broker.cluster
    initial-contact-points:
    {{- range (untilStep 0 (int .Values.orchestration.clusterSize) 1) }}
      - {{ include "orchestration.fullname" $ }}-{{ . }}.${K8S_SERVICE_NAME}:{{ $.Values.orchestration.service.internalPort }}
    {{- end }}

  api:
    grpc:
      address: 0.0.0.0
      port: {{ .Values.orchestration.service.grpcPort }}

  # Database configuration - Separated syntax.
  database:
  {{- if .Values.global.opensearch.enabled }}
    awsEnabled: {{ .Values.global.opensearch.aws.enabled }}
  {{- end }}

  data:
    snapshot-period: {{ .Values.orchestration.data.snapshotPeriod | quote }}
    primary-storage:
      disk:
        free-space:
          processing: {{ .Values.orchestration.data.disk.freeSpace.processing | quote }}
          replication: {{ .Values.orchestration.data.disk.freeSpace.replication | quote }}
    secondary-storage:
      autoconfigure-camunda-exporter: {{ .Values.orchestration.exporters.camunda.enabled }}
      type: {{ include "orchestration.secondaryStorage" . | quote -}}
      {{- if .Values.orchestration.history.retention.enabled }}
      retention:
        enabled: true
        minimum-age: {{ .Values.orchestration.history.retention.minimumAge | quote }}
      {{- end }}
      {{- if .Values.global.elasticsearch.enabled }}
      elasticsearch:
        {{- with .Values.orchestration.data.secondaryStorage.elasticsearch }}
        url: {{ .url | default (include "camundaPlatform.elasticsearchURL" $) | quote }}
        clusterName: {{ .clusterName | default $.Values.global.elasticsearch.clusterName | quote }}
        username: {{ .username | default $.Values.global.elasticsearch.auth.username | quote }}
        password: "${VALUES_ELASTICSEARCH_PASSWORD:}"
        indexPrefix: {{ .indexPrefix | default $.Values.orchestration.index.prefix | quote }}
        numberOfReplicas: {{ .numberOfReplicas | default $.Values.orchestration.index.replicas | quote }}
        {{- if .dateFormat }}
        dateFormat: {{ .dateFormat | quote }}
        {{- end }}
        {{- if .numberOfShards }}
        numberOfShards: {{ .numberOfShards | quote }}
        {{- end }}
        {{- if .variableSizeThreshold }}
        variableSizeThreshold: {{ .variableSizeThreshold }}
        {{- end }}
        {{- if .numberOfReplicasPerIndex }}
        numberOfReplicasPerIndex:
          {{- .numberOfReplicasPerIndex | toYaml | nindent 10 }}
        {{- end }}
        {{- if .numberOfShardsPerIndex }}
        numberOfShardsPerIndex:
          {{- .numberOfShardsPerIndex | toYaml | nindent 10 }}
        {{- end }}
        {{- if or .security.enabled (ne .security.verifyHostname nil) (ne .security.selfSigned nil) }}
        security:
          {{- if ne .security.enabled nil }}
          enabled: {{ .security.enabled }}
          {{- end }}
          {{- if ne .security.verifyHostname nil }}
          verifyHostname: {{ .security.verifyHostname }}
          {{- end }}
          {{- if ne .security.selfSigned nil }}
          selfSigned: {{ .security.selfSigned }}
          {{- end }}
        {{- end }}
        {{- if .interceptorPlugins }}
        interceptorPlugins:
          {{- .interceptorPlugins | toYaml | nindent 10 }}
        {{- end }}
        {{- if or $.Values.orchestration.history.retention.enabled .history.processInstanceEnabled .history.elsRolloverDateFormat .history.rolloverInterval .history.rolloverBatchSize .history.waitPeriodBeforeArchiving .history.delayBetweenRuns .history.maxDelayBetweenRuns .history.policyName }}
        history:
          {{- if $.Values.orchestration.history.retention.enabled }}
          policyName: {{ .history.policyName | default $.Values.orchestration.history.retention.policyName | quote }}
          {{- else if .history.policyName }}
          policyName: {{ .history.policyName | quote }}
          {{- end }}
          {{- if ne .history.processInstanceEnabled nil }}
          processInstanceEnabled: {{ .history.processInstanceEnabled }}
          {{- end }}
          {{- if .history.elsRolloverDateFormat }}
          elsRolloverDateFormat: {{ .history.elsRolloverDateFormat | quote }}
          {{- end }}
          {{- if .history.rolloverInterval }}
          rolloverInterval: {{ .history.rolloverInterval | quote }}
          {{- end }}
          {{- if .history.rolloverBatchSize }}
          rolloverBatchSize: {{ .history.rolloverBatchSize }}
          {{- end }}
          {{- if .history.waitPeriodBeforeArchiving }}
          waitPeriodBeforeArchiving: {{ .history.waitPeriodBeforeArchiving | quote }}
          {{- end }}
          {{- if .history.delayBetweenRuns }}
          delayBetweenRuns: {{ .history.delayBetweenRuns | quote }}
          {{- end }}
          {{- if .history.maxDelayBetweenRuns }}
          maxDelayBetweenRuns: {{ .history.maxDelayBetweenRuns | quote }}
          {{- end }}
        {{- end }}
        {{- if ne .createSchema nil }}
        createSchema: {{ .createSchema }}
        {{- end }}
        {{- if .incidentNotifier.auth0Protocol }}
        incidentNotifier:
          auth0Protocol: {{ .incidentNotifier.auth0Protocol | quote }}
        {{- end }}
        {{- if .batchOperationCache.maxSize }}
        batchOperationCache:
          maxSize: {{ .batchOperationCache.maxSize }}
        {{- end }}
        {{- if .processCache.maxSize }}
        processCache:
          maxSize: {{ .processCache.maxSize }}
        {{- end }}
        {{- if .formCache.maxSize }}
        formCache:
          maxSize: {{ .formCache.maxSize }}
        {{- end }}
        {{- if or .postExport.batchSize .postExport.delayBetweenRuns .postExport.maxDelayBetweenRuns (ne .postExport.ignoreMissingData nil) }}
        postExport:
          {{- if .postExport.batchSize }}
          batchSize: {{ .postExport.batchSize }}
          {{- end }}
          {{- if .postExport.delayBetweenRuns }}
          delayBetweenRuns: {{ .postExport.delayBetweenRuns | quote }}
          {{- end }}
          {{- if .postExport.maxDelayBetweenRuns }}
          maxDelayBetweenRuns: {{ .postExport.maxDelayBetweenRuns | quote }}
          {{- end }}
          {{- if ne .postExport.ignoreMissingData nil }}
          ignoreMissingData: {{ .postExport.ignoreMissingData }}
          {{- end }}
        {{- end }}
        {{- if ne .batchOperations.exportItemsOnCreation nil }}
        batchOperations:
          exportItemsOnCreation: {{ .batchOperations.exportItemsOnCreation }}
        {{- end }}
        {{- if or .bulk.delay .bulk.size .bulk.memoryLimit }}
        bulk:
          {{- if .bulk.delay }}
          delay: {{ .bulk.delay | quote }}
          {{- end }}
          {{- if .bulk.size }}
          size: {{ .bulk.size }}
          {{- end }}
          {{- if .bulk.memoryLimit }}
          memoryLimit: {{ .bulk.memoryLimit | quote }}
          {{- end }}
        {{- end }}
        {{- end }}
      {{- end }}
      {{- if .Values.global.opensearch.enabled }}
      opensearch:
        {{- with .Values.orchestration.data.secondaryStorage.opensearch }}
        url: {{ .url | default (include "camundaPlatform.opensearchURL" $) | quote }}
        clusterName: {{ .clusterName | default $.Values.global.opensearch.clusterName | quote }}
        username: {{ .username | default $.Values.global.opensearch.auth.username | quote }}
        password: "${VALUES_OPENSEARCH_PASSWORD:}"
        indexPrefix: {{ .indexPrefix | default $.Values.orchestration.index.prefix | quote }}
        numberOfReplicas: {{ .numberOfReplicas | default $.Values.orchestration.index.replicas | quote }}
        {{- if .dateFormat }}
        dateFormat: {{ .dateFormat | quote }}
        {{- end }}
        {{- if .numberOfShards }}
        numberOfShards: {{ .numberOfShards | quote }}
        {{- end }}
        {{- if .variableSizeThreshold }}
        variableSizeThreshold: {{ .variableSizeThreshold }}
        {{- end }}
        {{- if .numberOfReplicasPerIndex }}
        numberOfReplicasPerIndex:
          {{- .numberOfReplicasPerIndex | toYaml | nindent 10 }}
        {{- end }}
        {{- if .numberOfShardsPerIndex }}
        numberOfShardsPerIndex:
          {{- .numberOfShardsPerIndex | toYaml | nindent 10 }}
        {{- end }}
        {{- if or .security.enabled (ne .security.verifyHostname nil) (ne .security.selfSigned nil) }}
        security:
          {{- if ne .security.enabled nil }}
          enabled: {{ .security.enabled }}
          {{- end }}
          {{- if ne .security.verifyHostname nil }}
          verifyHostname: {{ .security.verifyHostname }}
          {{- end }}
          {{- if ne .security.selfSigned nil }}
          selfSigned: {{ .security.selfSigned }}
          {{- end }}
        {{- end }}
        {{- if .interceptorPlugins }}
        interceptorPlugins:
          {{- .interceptorPlugins | toYaml | nindent 10 }}
        {{- end }}
        {{- if or $.Values.orchestration.history.retention.enabled .history.processInstanceEnabled .history.elsRolloverDateFormat .history.rolloverInterval .history.rolloverBatchSize .history.waitPeriodBeforeArchiving .history.delayBetweenRuns .history.maxDelayBetweenRuns .history.policyName }}
        history:
          {{- if $.Values.orchestration.history.retention.enabled }}
          policyName: {{ .history.policyName | default $.Values.orchestration.history.retention.policyName | quote }}
          {{- else if .history.policyName }}
          policyName: {{ .history.policyName | quote }}
          {{- end }}
          {{- if ne .history.processInstanceEnabled nil }}
          processInstanceEnabled: {{ .history.processInstanceEnabled }}
          {{- end }}
          {{- if .history.elsRolloverDateFormat }}
          elsRolloverDateFormat: {{ .history.elsRolloverDateFormat | quote }}
          {{- end }}
          {{- if .history.rolloverInterval }}
          rolloverInterval: {{ .history.rolloverInterval | quote }}
          {{- end }}
          {{- if .history.rolloverBatchSize }}
          rolloverBatchSize: {{ .history.rolloverBatchSize }}
          {{- end }}
          {{- if .history.waitPeriodBeforeArchiving }}
          waitPeriodBeforeArchiving: {{ .history.waitPeriodBeforeArchiving | quote }}
          {{- end }}
          {{- if .history.delayBetweenRuns }}
          delayBetweenRuns: {{ .history.delayBetweenRuns | quote }}
          {{- end }}
          {{- if .history.maxDelayBetweenRuns }}
          maxDelayBetweenRuns: {{ .history.maxDelayBetweenRuns | quote }}
          {{- end }}
        {{- end }}
        {{- if ne .createSchema nil }}
        createSchema: {{ .createSchema }}
        {{- end }}
        {{- if .incidentNotifier.auth0Protocol }}
        incidentNotifier:
          auth0Protocol: {{ .incidentNotifier.auth0Protocol | quote }}
        {{- end }}
        {{- if .batchOperationCache.maxSize }}
        batchOperationCache:
          maxSize: {{ .batchOperationCache.maxSize }}
        {{- end }}
        {{- if .processCache.maxSize }}
        processCache:
          maxSize: {{ .processCache.maxSize }}
        {{- end }}
        {{- if .formCache.maxSize }}
        formCache:
          maxSize: {{ .formCache.maxSize }}
        {{- end }}
        {{- if or .postExport.batchSize .postExport.delayBetweenRuns .postExport.maxDelayBetweenRuns (ne .postExport.ignoreMissingData nil) }}
        postExport:
          {{- if .postExport.batchSize }}
          batchSize: {{ .postExport.batchSize }}
          {{- end }}
          {{- if .postExport.delayBetweenRuns }}
          delayBetweenRuns: {{ .postExport.delayBetweenRuns | quote }}
          {{- end }}
          {{- if .postExport.maxDelayBetweenRuns }}
          maxDelayBetweenRuns: {{ .postExport.maxDelayBetweenRuns | quote }}
          {{- end }}
          {{- if ne .postExport.ignoreMissingData nil }}
          ignoreMissingData: {{ .postExport.ignoreMissingData }}
          {{- end }}
        {{- end }}
        {{- if ne .batchOperations.exportItemsOnCreation nil }}
        batchOperations:
          exportItemsOnCreation: {{ .batchOperations.exportItemsOnCreation }}
        {{- end }}
        {{- if or .bulk.delay .bulk.size .bulk.memoryLimit }}
        bulk:
          {{- if .bulk.delay }}
          delay: {{ .bulk.delay | quote }}
          {{- end }}
          {{- if .bulk.size }}
          size: {{ .bulk.size }}
          {{- end }}
          {{- if .bulk.memoryLimit }}
          memoryLimit: {{ .bulk.memoryLimit | quote }}
          {{- end }}
        {{- end }}
        {{- end }}
      {{- end }}
      {{- if .Values.orchestration.exporters.rdbms.enabled }}
      # RDBMS exporter is configured and enabled automatically when type is rdbms
      rdbms:
        {{- with .Values.orchestration.data.secondaryStorage.rdbms }}
        url: {{ .url | quote }}
        username: {{ .username | quote }}
        password: "${VALUES_ORCHESTRATION_DATA_SECONDARYSTORAGE_RDBMS_PASSWORD:}"
        {{- if .flushInterval }}
        flushInterval: {{ .flushInterval | quote }}
        {{- end }}
        {{- if .queueSize }}
        queueSize: {{ .queueSize | quote }}
        {{- end }}
        {{- if .queueMemoryLimit }}
        queueMemoryLimit: {{ .queueMemoryLimit | quote }}
        {{- end }}
        auto-ddl: {{ .autoDDL }}
        {{- if .prefix }}
        prefix: {{ .prefix | quote }}
        {{- end }}
        {{- if .maxQueueSize }}
        maxQueueSize: {{ .maxQueueSize | quote }}
        {{- end }}
        history:
          {{- if .history.defaultHistoryTTL }}
          defaultHistoryTTL: {{ .history.defaultHistoryTTL | quote }}
          {{- end }}
          {{- if .history.defaultBatchOperationHistoryTTL }}
          defaultBatchOperationHistoryTTL: {{ .history.defaultBatchOperationHistoryTTL | quote }}
          {{- end }}
          {{- if .history.batchOperationCancelProcessInstanceHistoryTTL }}
          batchOperationCancelProcessInstanceHistoryTTL: {{ .history.batchOperationCancelProcessInstanceHistoryTTL | quote }}
          {{- end }}
          {{- if .history.batchOperationMigrateProcessInstanceHistoryTTL }}
          batchOperationMigrateProcessInstanceHistoryTTL: {{ .history.batchOperationMigrateProcessInstanceHistoryTTL | quote }}
          {{- end }}
          {{- if .history.batchOperationModifyProcessInstanceHistoryTTL }}
          batchOperationModifyProcessInstanceHistoryTTL: {{ .history.batchOperationModifyProcessInstanceHistoryTTL | quote }}
          {{- end }}
          {{- if .history.batchOperationResolveIncidentHistoryTTL }}
          batchOperationResolveIncidentHistoryTTL: {{ .history.batchOperationResolveIncidentHistoryTTL | quote }}
          {{- end }}
          {{- if .history.minHistoryCleanupInterval }}
          minHistoryCleanupInterval: {{ .history.minHistoryCleanupInterval | quote }}
          {{- end }}
          {{- if .history.maxHistoryCleanupInterval }}
          maxHistoryCleanupInterval: {{ .history.maxHistoryCleanupInterval | quote }}
          {{- end }}
          {{- if .history.historyCleanupBatchSize }}
          historyCleanupBatchSize: {{ .history.historyCleanupBatchSize | quote }}
          {{- end }}
          {{- if .history.usageMetricsCleanup }}
          usageMetricsCleanup: {{ .history.usageMetricsCleanup | quote }}
          {{- end }}
          {{- if .history.processCache.maxSize }}
          processCache:
            maxSize: {{ .history.processCache.maxSize | quote }}
          {{- end }}
          {{- if .history.batchOperationCache.maxSize }}
          batchOperationCache:
            maxSize: {{ .history.batchOperationCache.maxSize | quote }}
          {{- end }}
          connection-pool:
            {{- if .history.connectionPool.maximumPoolSize }}
            maximumPoolSize: {{ .history.connectionPool.maximumPoolSize | quote }}
            {{- end }}
            {{- if .history.connectionPool.minimumIdle }}
            minimumIdle: {{ .history.connectionPool.minimumIdle | quote }}
            {{- end }}
            {{- if .history.connectionPool.idleTimeout }}
            idleTimeout: {{ .history.connectionPool.idleTimeout | quote }}
            {{- end }}
            {{- if .history.connectionPool.maxLifetime }}
            maxLifetime: {{ .history.connectionPool.maxLifetime | quote }}
            {{- end }}
            {{- if .history.connectionPool.connectionTimeout }}
            connectionTimeout: {{ .history.connectionPool.connectionTimeout | quote }}
            {{- end }}
        {{- end }}
      {{- end }}

  # Security configuration - Separated syntax.
  security:
    authentication:
    {{- if eq (include "orchestration.authMethod" .) "oidc" }}
      oidc:
        username-claim: {{ .Values.orchestration.security.authentication.oidc.usernameClaim | quote }}
        client-id-claim: {{ .Values.orchestration.security.authentication.oidc.clientIdClaim | quote }}
        groups-claim: {{ .Values.orchestration.security.authentication.oidc.groupsClaim | quote }}
        client-id: {{ include "orchestration.authClientId" . | quote }}
        client-secret: ${VALUES_ORCHESTRATION_CLIENT_SECRET:}
        audiences:
          {{- range .Values.orchestration.security.authentication.oidc.backwardsCompatibleAudiences }}
          - {{ . | quote }}
          {{- end }}
          - {{ include "orchestration.authClientId" . | quote }}
          - {{ include "orchestration.authAudience" . | quote }}
          - {{ .Values.global.identity.auth.webModeler.clientApiAudience | quote }}
        {{- if (include "orchestration.authIssuerUrl" .) }}
        issuer-uri: {{ include "orchestration.authIssuerUrl" . | quote }}
        {{- else }}
        authorization-uri: {{ include "orchestration.authIssuerUrlEndpointAuth" . | quote }}
        jwk-set-uri: {{ include "orchestration.authIssuerBackendUrlEndpointCerts" . | quote }}
        token-uri: {{ include "orchestration.authIssuerBackendUrlEndpointToken" . | quote }}
        {{- end }}
        redirect-uri: {{ printf "%s/sso-callback" (include "orchestration.RedirectURI" .) | quote }}
        authenticationRefreshInterval: {{ .Values.orchestration.security.authentication.authenticationRefreshInterval | quote }}
        {{- if .Values.orchestration.security.authentication.oidc.preferUsernameClaim }}
        prefer-username-claim: {{ .Values.orchestration.security.authentication.oidc.preferUsernameClaim }}
        {{- end }}
        {{- if .Values.orchestration.security.authentication.oidc.scope }}
        scope:
          {{- include "common.tplvalues.render" ( dict "value" .Values.orchestration.security.authentication.oidc.scope "context" $ ) | nindent 10 }}
        {{- end }}
      {{- end }}
      method: {{ include "orchestration.authMethod" . | quote }}
      unprotectedApi: {{ .Values.orchestration.security.authentication.unprotectedApi }}
    authorizations:
      enabled: {{ .Values.orchestration.security.authorizations.enabled }}
    initialization:
      default-roles:
        {{- .Values.orchestration.security.initialization.defaultRoles | toYaml | nindent 8 }}
      {{- if eq (include "orchestration.authMethod" .) "basic" }}
      users:
        {{- .Values.orchestration.security.initialization.users | toYaml | nindent 8 }}
      {{- end }}
    multiTenancy:
      checksEnabled: {{ include "orchestration.multitenancyChecksEnabled" . }}
      apiEnabled: {{ include "orchestration.multitenancyApiEnabled" . }}

  {{- if eq (include "orchestration.authMethod" .) "oidc" }}
  identity:
    clientId: {{ include "orchestration.authClientId" . | quote }}
    audience: {{ include "orchestration.authAudience" . | quote }}
  {{- end }}

{{- if .Values.orchestration.profiles.operate -}}
  {{- "\n" }}
  #
  # Camunda Operate Configuration - Separated syntax.
  #
  operate:
    persistentSessionsEnabled: {{ include "orchestration.persistentSessionsEnabled" . }}
    multiTenancy:
      enabled: {{ include "orchestration.multitenancyChecksEnabled" . }}
    {{- if eq (include "orchestration.authMethod" .) "oidc" }}
    identity:
      redirectRootUrl: {{ printf "%s/operate" (include "orchestration.RedirectURI" .) | quote }}
    {{- end }}
    {{- if .Values.global.opensearch.enabled }}
    # OpenSearch
    opensearch:
      awsEnabled: {{ .Values.global.opensearch.aws.enabled }}
    zeebeOpensearch:
      awsEnabled: {{ .Values.global.opensearch.aws.enabled }}
    {{- end }}
    # Zeebe instance
    zeebe:
      # Gateway address
      gatewayAddress: {{ include "orchestration.serviceNameGRPC" . | quote }}
    {{- if .Values.orchestration.history.retention.enabled }}
    archiver:
      ilmEnabled: true
      ilmMinAgeForDeleteArchivedIndices: {{ .Values.orchestration.history.retention.minimumAge }}
    {{- end }}
{{- end -}}

{{- if .Values.orchestration.profiles.tasklist -}}
  {{- "\n" }}
  #
  # Camunda Tasklist Configuration - Separated syntax.
  #
  tasklist:
    multiTenancy:
      enabled: {{ include "orchestration.multitenancyChecksEnabled" . }}

    {{- if eq (include "orchestration.authMethod" .) "oidc" }}
    identity:
      redirectRootUrl: {{ printf "%s/tasklist" (include "orchestration.RedirectURI" .) | quote }}
    {{- end }}
    {{- if .Values.global.opensearch.enabled }}
    # OpenSearch
    opensearch:
      awsEnabled: {{ .Values.global.opensearch.aws.enabled }}
    zeebeOpensearch:
      awsEnabled: {{ .Values.global.opensearch.aws.enabled }}
      username: {{ .Values.global.opensearch.auth.username | quote }}
      password: "${VALUES_OPENSEARCH_PASSWORD:}"
    {{- end }}
    # Zeebe instance
    zeebe:
      # Gateway address
      gatewayAddress: {{ include "orchestration.serviceNameGRPC" . | quote }}
      restAddress: {{ include "camundaPlatform.orchestrationHTTPInternalURL" . | quote }}
    {{- if .Values.orchestration.history.retention.enabled }}
    archiver:
      ilmEnabled: true
      ilmMinAgeForDeleteArchivedIndices: {{ .Values.orchestration.history.retention.minimumAge }}
    {{- end }}
{{- end -}}

{{- if .Values.orchestration.profiles.broker -}}
{{- "\n" }}
#
# Camunda Zeebe Configuration - Separated syntax.
#
zeebe:
  host: 0.0.0.0
  log:
    level: {{ .Values.orchestration.logLevel | quote }}

  broker:
    # zeebe.broker.gateway
    gateway:
      enable: true
      multitenancy:
        enabled: {{ include "orchestration.multitenancyChecksEnabled" . }}

    # zeebe.broker.network
    network:
      {{- if gt (int .Values.global.multiregion.regions) 1 }}
      advertisedHost: "${K8S_NAME}.${K8S_SERVICE_NAME}.${K8S_NAMESPACE}.svc"
      {{- else }}
      advertisedHost: "${K8S_NAME}.${K8S_SERVICE_NAME}"
      {{- end }}
      host: 0.0.0.0
      commandApi:
        port: {{ .Values.orchestration.service.commandPort }}
      internalApi:
        port: {{ .Values.orchestration.service.internalPort }}

    # zeebe.broker.cluster
    cluster:
      clusterName: {{ tpl .Values.global.zeebeClusterName . }}

    # zeebe.broker.exporters
    exporters:
    {{- if and
      (or
        (and .Values.global.elasticsearch.enabled .Values.orchestration.exporters.zeebe.enabled)
        (and .Values.global.elasticsearch.enabled .Values.optimize.enabled)
      )
      (or
        .Values.orchestration.exporters.zeebe.enabled
        (lt (int (default 0 .Values.global.multiregion.regions)) 2)
      )
    }}
      elasticsearch:
        className: "io.camunda.zeebe.exporter.ElasticsearchExporter"
        args:
          {{- if .Values.global.elasticsearch.external }}
          authentication:
            username: {{ .Values.global.elasticsearch.auth.username | quote }}
            password: "${VALUES_ELASTICSEARCH_PASSWORD:}"
          {{- end }}
          url: {{ include "camundaPlatform.elasticsearchURL" . | quote }}
          index:
            prefix: {{ .Values.global.elasticsearch.prefix | quote }}
          {{- if .Values.orchestration.retention.enabled }}
          retention:
            enabled: true
            minimumAge: {{ .Values.orchestration.retention.minimumAge | quote }}
            policyName: {{ .Values.orchestration.retention.policyName | quote }}
          {{- end }}
    {{- else if and
      (or
        (and .Values.global.opensearch.enabled .Values.orchestration.exporters.zeebe.enabled)
        (and .Values.global.opensearch.enabled .Values.optimize.enabled)
      )
      (or
        .Values.orchestration.exporters.zeebe.enabled
        (lt (int (default 0 .Values.global.multiregion.regions)) 2)
      )
    }}
      opensearch:
        className: "io.camunda.zeebe.exporter.opensearch.OpensearchExporter"
        args:
          url: {{ include "camundaPlatform.opensearchURL" . | quote }}
          index:
            prefix: {{ .Values.global.opensearch.prefix | quote }}
          {{- if .Values.global.opensearch.aws.enabled }}
          aws:
            enabled: true
          {{- end }}
          {{- if .Values.orchestration.retention.enabled }}
          retention:
            enabled: true
            minimumAge: {{ .Values.orchestration.retention.minimumAge | quote }}
            policyName: {{ .Values.orchestration.retention.policyName | quote }}
          {{- end }}
      {{- end }}
    {{- if and (not (eq (include "orchestration.secondaryStorage" .) "none")) .Values.orchestration.exporters.camunda.enabled (not .Values.orchestration.exporters.rdbms.enabled) }}
      camundaexporter:
        className: "io.camunda.exporter.CamundaExporter"
        args:
          connect:
            type: {{ include "orchestration.secondaryStorage" . | quote }}
            {{- if .Values.orchestration.index.prefix }}
            indexPrefix: {{ .Values.orchestration.index.prefix | quote }}
            {{- end }}
            awsEnabled: {{ .Values.global.opensearch.aws.enabled }}
          history:
            elsRolloverDateFormat: {{ .Values.orchestration.history.elsRolloverDateFormat | quote }}
            rolloverInterval: {{ .Values.orchestration.history.rolloverInterval | quote }}
            rolloverBatchSize: {{ .Values.orchestration.history.rolloverBatchSize }}
            waitPeriodBeforeArchiving: {{ .Values.orchestration.history.waitPeriodBeforeArchiving | quote }}
            delayBetweenRuns: {{ .Values.orchestration.history.delayBetweenRuns }}
            maxDelayBetweenRuns: {{ .Values.orchestration.history.maxDelayBetweenRuns }}
    {{- end }}
{{ end -}}
