global:
  identity:
    auth:
      # Disable the Identity authentication for local development
      # it will fall back to basic-auth: demo/demo as default user
      enabled: false

# Disable identity as part of the Camunda core
identity:
  enabled: false

# Disable keycloak
identityKeycloak:
  enabled: false

optimize:
  enabled: false

operate:
  resources: {}

tasklist:
  resources: {}

# Reduce for Zeebe and Gateway the configured replicas and with that the required resources
# to get it running locally
zeebe:
  resources: {}
  clusterSize: 1
  partitionCount: 1
  replicationFactor: 1
  pvcSize: 10Gi

zeebeGateway:
  resources: {}
  replicas: 1

connectors:
  # Disable connectors for local development as it doesn't support the "disabled" mode.
  enabled: false
  inbound:
    mode: disabled

elasticsearch:
  master:
    resources: {}
    replicaCount: 1
    # Request smaller persistent volumes.
    persistence:
      size: 10Gi
