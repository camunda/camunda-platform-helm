apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: -health

resources:
- ../base

configMapGenerator:
- name: venom-tests
  options:
    disableNameSuffixHash: true
  files:
  - testcases/health.yaml
