apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- job.yaml

configMapGenerator:
- name: venom-env-vars
  options:
    disableNameSuffixHash: true
  envs:
  - config/venom-env-vars
