diff --git a/charts/camunda-platform-8.10/templates/common/constraints.tpl b/charts/camunda-platform-8.10/templates/common/constraints.tpl
index da01311c688..99a322117b5 100644
--- a/charts/camunda-platform-8.10/templates/common/constraints.tpl
+++ b/charts/camunda-platform-8.10/templates/common/constraints.tpl
@@ -294,19 +294,28 @@ The following values inside your values.yaml need to be set but were not:
     {{ printf "\n%s" $warningMessage | trimSuffix "\n" }}
   {{- end }}
 
-  {{/* global.elasticsearch and global.opensearch config warnings */}}
+  {{/* Legacy per-component JKS truststore deprecation
+       (in favour of `global.tls.caBundle.secret.*` PEM bundle, which the
+       chart converts to a PKCS12 truststore at pod start via the caBundle
+       init container — see helm#3498). */}}
   {{- $deprecatedDatabaseTlsOptions := list
   (dict "path" "global.elasticsearch.tls.secret" "config" .Values.global.elasticsearch.tls.secret)
   (dict "path" "global.opensearch.tls.secret" "config" .Values.global.opensearch.tls.secret)
+  (dict "path" "global.elasticsearch.tls.jks.secret" "config" (.Values.global.elasticsearch.tls.jks).secret)
+  (dict "path" "global.opensearch.tls.jks.secret" "config" (.Values.global.opensearch.tls.jks).secret)
+  (dict "path" "orchestration.data.secondaryStorage.elasticsearch.tls.secret" "config" .Values.orchestration.data.secondaryStorage.elasticsearch.tls.secret)
+  (dict "path" "orchestration.data.secondaryStorage.opensearch.tls.secret" "config" .Values.orchestration.data.secondaryStorage.opensearch.tls.secret)
+  (dict "path" "optimize.database.elasticsearch.tls.secret" "config" .Values.optimize.database.elasticsearch.tls.secret)
+  (dict "path" "optimize.database.opensearch.tls.secret" "config" .Values.optimize.database.opensearch.tls.secret)
   }}
   {{- range $deprecatedDatabaseTlsOptions }}
     {{- if (eq (include "camundaPlatform.hasSecretConfig" (dict "config" .config)) "true") }}
         {{- $warningMessage := printf "%s %s %s %s %s"
             "[camunda][warning]"
-            (printf "DEPRECATION: values.yaml is using legacy option '%s'." .path)
-            "This option is deprecated and will be removed in a future version."
-            (printf "Please migrate to the new option: 'orchestration.data.secondaryStorage.(elasticsearch|opensearch).tls.secret.existingSecret'")
-            (printf "or for optimize: 'optimize.database.(elasticsearch|opensearch).tls.secret.existingSecret'")
+            (printf "DEPRECATION: values.yaml is using legacy JKS truststore option '%s'." .path)
+            "This option is deprecated as of chart 15.x and will be removed in a future major release."
+            "Please migrate to 'global.tls.caBundle.secret.{existingSecret,existingSecretKey}', supplying a PEM-encoded CA bundle."
+            "The chart will build the JVM truststore at pod start (no offline keytool needed). See https://docs.camunda.io/docs/self-managed/deployment/helm/configure/tls#legacy-per-component-jks-truststore-deprecated for the migration recipe."
         -}}
         {{ printf "\n%s" $warningMessage | trimSuffix "\n" }}
     {{- end }}
diff --git a/charts/camunda-platform-8.10/test/unit/common/constraints_test.go b/charts/camunda-platform-8.10/test/unit/common/constraints_test.go
index af3da02a565..aaaee41e834 100644
--- a/charts/camunda-platform-8.10/test/unit/common/constraints_test.go
+++ b/charts/camunda-platform-8.10/test/unit/common/constraints_test.go
@@ -218,6 +218,63 @@ func (s *ConstraintTemplateTest) TestPusherSecretConstraint() {
 }
 
 
+// TestLegacyJksTruststoreFieldsRenderWithoutCrash asserts that setting any
+// of the legacy JKS truststore fields targeted by the chart-15.x
+// deprecation does not break template rendering. The deprecation warning
+// itself is emitted via NOTES.txt at install time and is not surfaced by
+// `helm template` (the framework these tests use), so warning-content
+// assertions live only in manual `helm install --dry-run` / production
+// install verification — same constraint that applies to the existing
+// global.elasticsearch.tls.secret and Bitnami subchart deprecation
+// warnings.
+func (s *ConstraintTemplateTest) TestLegacyJksTruststoreFieldsRenderWithoutCrash() {
+	testCases := []testhelpers.TestCase{
+		{
+			Name: "TestSecondaryStorageElasticsearchTlsSecretRendersOk",
+			Values: map[string]string{
+				"orchestration.data.secondaryStorage.type":                                    "elasticsearch",
+				"orchestration.data.secondaryStorage.elasticsearch.tls.secret.existingSecret": "my-legacy-jks",
+			},
+			Verifier: func(t *testing.T, output string, err error) {
+				s.Require().Nil(err)
+			},
+		},
+		{
+			Name: "TestSecondaryStorageOpensearchTlsSecretRendersOk",
+			Values: map[string]string{
+				"orchestration.data.secondaryStorage.type":                                 "opensearch",
+				"orchestration.data.secondaryStorage.opensearch.tls.secret.existingSecret": "my-legacy-jks",
+			},
+			Verifier: func(t *testing.T, output string, err error) {
+				s.Require().Nil(err)
+			},
+		},
+		{
+			Name: "TestOptimizeElasticsearchTlsSecretRendersOk",
+			Values: map[string]string{
+				"optimize.database.elasticsearch.tls.secret.existingSecret": "my-legacy-jks",
+			},
+			Verifier: func(t *testing.T, output string, err error) {
+				s.Require().Nil(err)
+			},
+		},
+		{
+			Name: "TestCaBundleAndLegacyJksCoexistRenderOk",
+			Values: map[string]string{
+				"orchestration.data.secondaryStorage.type":                                    "elasticsearch",
+				"orchestration.data.secondaryStorage.elasticsearch.tls.secret.existingSecret": "my-legacy-jks",
+				"global.tls.caBundle.secret.existingSecret":                                   "camunda-ca-bundle",
+			},
+			Verifier: func(t *testing.T, output string, err error) {
+				s.Require().Nil(err)
+			},
+		},
+	}
+
+	testhelpers.RunTestCasesE(s.T(), s.chartPath, s.release, s.namespace, s.templates, testCases)
+}
+
+
 func (s *ConstraintTemplateTest) TestBitnamiSubchartDeprecationWarnings() {
 	testCases := []testhelpers.TestCase{
 		{
diff --git a/charts/camunda-platform-8.10/values.schema.json b/charts/camunda-platform-8.10/values.schema.json
index b743beaebe7..1d80a6b51ca 100644
--- a/charts/camunda-platform-8.10/values.schema.json
+++ b/charts/camunda-platform-8.10/values.schema.json
@@ -5456,12 +5456,12 @@
                                                     "properties": {
                                                         "existingSecret": {
                                                             "type": "string",
-                                                            "description": "can be used to reference an existing Kubernetes Secret containing the TLS trust store",
+                                                            "description": "(DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecret) can be used to reference an existing Kubernetes Secret containing the TLS trust store",
                                                             "default": ""
                                                         },
                                                         "existingSecretKey": {
                                                             "type": "string",
-                                                            "description": "defines the key within the existing Kubernetes Secret for TLS trust store",
+                                                            "description": "(DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecretKey) defines the key within the existing Kubernetes Secret for TLS trust store",
                                                             "default": ""
                                                         }
                                                     }
@@ -5526,12 +5526,12 @@
                                                     "properties": {
                                                         "existingSecret": {
                                                             "type": "string",
-                                                            "description": "can be used to reference an existing Kubernetes Secret containing the TLS trust store",
+                                                            "description": "(DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecret) can be used to reference an existing Kubernetes Secret containing the TLS trust store",
                                                             "default": ""
                                                         },
                                                         "existingSecretKey": {
                                                             "type": "string",
-                                                            "description": "defines the key within the existing Kubernetes Secret for TLS trust store",
+                                                            "description": "(DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecretKey) defines the key within the existing Kubernetes Secret for TLS trust store",
                                                             "default": ""
                                                         }
                                                     }
@@ -6096,12 +6096,12 @@
                                             "properties": {
                                                 "existingSecret": {
                                                     "type": "string",
-                                                    "description": "can be used to provide the name of an existing Kubernetes secret containing TLS certificate",
+                                                    "description": "(DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecret) can be used to provide the name of an existing Kubernetes secret containing TLS certificate",
                                                     "default": ""
                                                 },
                                                 "existingSecretKey": {
                                                     "type": "string",
-                                                    "description": "can be used to provide the key within the secret containing the certificate",
+                                                    "description": "(DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecretKey) can be used to provide the key within the secret containing the certificate",
                                                     "default": "externaldb.jks"
                                                 }
                                             }
@@ -6196,12 +6196,12 @@
                                             "properties": {
                                                 "existingSecret": {
                                                     "type": "string",
-                                                    "description": "can be used to provide the name of an existing Kubernetes secret containing TLS certificate",
+                                                    "description": "(DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecret) can be used to provide the name of an existing Kubernetes secret containing TLS certificate",
                                                     "default": ""
                                                 },
                                                 "existingSecretKey": {
                                                     "type": "string",
-                                                    "description": "can be used to provide the key within the secret containing the certificate",
+                                                    "description": "(DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecretKey) can be used to provide the key within the secret containing the certificate",
                                                     "default": "externaldb.jks"
                                                 }
                                             }
diff --git a/charts/camunda-platform-8.10/values.yaml b/charts/camunda-platform-8.10/values.yaml
index 78cb8923c50..182a032d6d1 100755
--- a/charts/camunda-platform-8.10/values.yaml
+++ b/charts/camunda-platform-8.10/values.yaml
@@ -2862,11 +2862,11 @@ orchestration:
             existingSecretKey: ""
         ## @extra orchestration.data.secondaryStorage.elasticsearch.tls configuration for TLS settings for elasticsearch connection
         tls:
-          ## @extra orchestration.data.secondaryStorage.elasticsearch.tls.secret secret ref for TLS trust store
+          ## @extra orchestration.data.secondaryStorage.elasticsearch.tls.secret (DEPRECATED in chart 15.x — use global.tls.caBundle.secret with a PEM CA bundle; the chart builds the JVM truststore at pod start) secret ref for TLS trust store
           secret:
-            ## @param orchestration.data.secondaryStorage.elasticsearch.tls.secret.existingSecret can be used to reference an existing Kubernetes Secret containing the TLS trust store
+            ## @param orchestration.data.secondaryStorage.elasticsearch.tls.secret.existingSecret (DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecret) can be used to reference an existing Kubernetes Secret containing the TLS trust store
             existingSecret: ""
-            ## @param orchestration.data.secondaryStorage.elasticsearch.tls.secret.existingSecretKey defines the key within the existing Kubernetes Secret for TLS trust store
+            ## @param orchestration.data.secondaryStorage.elasticsearch.tls.secret.existingSecretKey (DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecretKey) defines the key within the existing Kubernetes Secret for TLS trust store
             existingSecretKey: ""
       ## @extra orchestration.data.secondaryStorage.opensearch configuration for Opensearch as secondary storage
       opensearch:
@@ -2890,11 +2890,11 @@ orchestration:
             existingSecretKey: ""
         ## @extra orchestration.data.secondaryStorage.opensearch.tls configuration for TLS settings for opensearch connection
         tls:
-          ## @extra orchestration.data.secondaryStorage.opensearch.tls.secret secret ref for TLS trust store
+          ## @extra orchestration.data.secondaryStorage.opensearch.tls.secret (DEPRECATED in chart 15.x — use global.tls.caBundle.secret with a PEM CA bundle; the chart builds the JVM truststore at pod start) secret ref for TLS trust store
           secret:
-            ## @param orchestration.data.secondaryStorage.opensearch.tls.secret.existingSecret can be used to reference an existing Kubernetes Secret containing the TLS trust store
+            ## @param orchestration.data.secondaryStorage.opensearch.tls.secret.existingSecret (DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecret) can be used to reference an existing Kubernetes Secret containing the TLS trust store
             existingSecret: ""
-            ## @param orchestration.data.secondaryStorage.opensearch.tls.secret.existingSecretKey defines the key within the existing Kubernetes Secret for TLS trust store
+            ## @param orchestration.data.secondaryStorage.opensearch.tls.secret.existingSecretKey (DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecretKey) defines the key within the existing Kubernetes Secret for TLS trust store
             existingSecretKey: ""
   ## @param orchestration.extraVolumes can be used to define extra volumes for the broker pods, useful for additional exporters
   extraVolumes: []
@@ -3162,11 +3162,11 @@ optimize:
       external: false
       tls:
         enabled: false
-        ## @extra optimize.database.elasticsearch.tls.secret configuration for new secret pattern (>= 8.9)
+        ## @extra optimize.database.elasticsearch.tls.secret (DEPRECATED in chart 15.x — use global.tls.caBundle.secret with a PEM CA bundle; the chart builds the JVM truststore at pod start) configuration for new secret pattern (>= 8.9)
         secret:
-          ## @param optimize.database.elasticsearch.tls.secret.existingSecret can be used to provide the name of an existing Kubernetes secret containing TLS certificate
+          ## @param optimize.database.elasticsearch.tls.secret.existingSecret (DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecret) can be used to provide the name of an existing Kubernetes secret containing TLS certificate
           existingSecret: ""
-          ## @param optimize.database.elasticsearch.tls.secret.existingSecretKey can be used to provide the key within the secret containing the certificate
+          ## @param optimize.database.elasticsearch.tls.secret.existingSecretKey (DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecretKey) can be used to provide the key within the secret containing the certificate
           existingSecretKey: "externaldb.jks"
       auth:
         username: ""
@@ -3202,11 +3202,11 @@ optimize:
         enabled: false
       tls:
         enabled: false
-        ## @extra optimize.database.opensearch.tls.secret configuration for new secret pattern (>= 8.9)
+        ## @extra optimize.database.opensearch.tls.secret (DEPRECATED in chart 15.x — use global.tls.caBundle.secret with a PEM CA bundle; the chart builds the JVM truststore at pod start) configuration for new secret pattern (>= 8.9)
         secret:
-          ## @param optimize.database.opensearch.tls.secret.existingSecret can be used to provide the name of an existing Kubernetes secret containing TLS certificate
+          ## @param optimize.database.opensearch.tls.secret.existingSecret (DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecret) can be used to provide the name of an existing Kubernetes secret containing TLS certificate
           existingSecret: ""
-          ## @param optimize.database.opensearch.tls.secret.existingSecretKey can be used to provide the key within the secret containing the certificate
+          ## @param optimize.database.opensearch.tls.secret.existingSecretKey (DEPRECATED in chart 15.x — use global.tls.caBundle.secret.existingSecretKey) can be used to provide the key within the secret containing the certificate
           existingSecretKey: "externaldb.jks"
       auth:
         username: ""
